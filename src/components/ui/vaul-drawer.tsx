import * as React from "react";
import * as DrawerPrimitive from "vaul";

// We're only exporting DrawerPortal from here to avoid naming conflicts
const DrawerPortal = DrawerPrimitive.Portal;

export { DrawerPortal };
